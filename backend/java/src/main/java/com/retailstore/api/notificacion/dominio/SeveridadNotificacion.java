package com.retailstore.api.notificacion.dominio;

public enum SeveridadNotificacion {
    /** Ha pasado algo que conviene saber. */
    INFO,
    /** Hay que mirarlo hoy. */
    AVISO,
    /** Está costando ventas ahora mismo. */
    URGENTE
}
